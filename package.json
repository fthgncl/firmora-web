{
  "name": "firmora-web",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://fthgncl.github.io/firmora",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.9.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "buildAndPush": "react-scripts build && powershell -NoProfile -ExecutionPolicy Bypass -Command \"& { $dest='E:\\Software\\firmora'; $src='build'; if (-not (Test-Path \\\"$dest\\.git\\\")) { throw 'Hedef git repo değil' }; Get-ChildItem -LiteralPath $dest -Force | ? { $_.Name -ne '.git' } | Remove-Item -Recurse -Force; Copy-Item -Path \\\"$src\\*\\\" -Destination $dest -Recurse -Force -Container; git -C $dest add -A; $diff = git -C $dest status --porcelain; if ([string]::IsNullOrWhiteSpace($diff)) { Write-Host 'No changes, skipping commit/push.'; exit 0 }; $stamp=Get-Date -Format 'yyyy-MM-dd HH:mm'; git -C $dest commit -m \\\"auto build: $stamp\\\"; git -C $dest push }\"",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
